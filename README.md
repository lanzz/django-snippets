Assorted Django snippets
========================

#### `models.FixedPointField(verbose_name=None, name=None, decimal_places=2)`

Fixed-point field, stored as integer in the database, represented as `decimal.Decimal` in Python.
